# PDF 处理
PyMuPDF>=1.23.0

# LLM (OpenAI 兼容接口，支持 Ollama/DeepSeek 等)
openai>=1.0.0

# 配置
pyyaml>=6.0

# Embedding
sentence-transformers>=2.2.0

# BM25 分词
jieba>=0.42

# 测试
pytest>=7.0.0

# OCR
pytesseract>=0.3.0  # 轻量：需要 brew install tesseract

# 表格提取（可选，图像预处理）
opencv-python>=4.8.0
Pillow>=10.0.0
