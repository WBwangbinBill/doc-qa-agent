# PDF 处理
PyMuPDF>=1.23.0

# LLM
openai>=1.0.0

# 配置
pyyaml>=6.0

# HTTP (Embedding 服务)
requests>=2.28.0

# 测试
pytest>=7.0.0

# OCR (二选一)
# paddleocr>=2.7.0  # 推荐：中文识别好，但安装较大
pytesseract  # 轻量：需要 brew install tesseract

# 可选（文本版 PDF 不需要）
# opencv-python>=4.8.0
# Pillow>=10.0.0
